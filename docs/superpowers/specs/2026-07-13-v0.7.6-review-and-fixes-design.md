# v0.7.6 全项目 Review 与最小修复方案

> 阅读对象: 仅作者, 不属于用户文档。**不修代码**直到本设计被确认。

## 1. 范围与意图

对当前 `main` (`v0.7.6`) 仓库做全项目 review, 整合后端/前端/安全/性能/冗余 (含自检) 五个维度的发现, 只做**最小且必要**的修复, 保留 v0.7.6 的发布形态。

原则 (Ponytail full 模式 + anti-slop):

- **不重写, 只修真实 bug / 明确死代码 / 明显安全洞**。
- **P0 必修**; P1 推荐; P2 入 known-issues, 不下手。
- 每个改动 ≤ ~10 行, 改完跑 `npx vue-tsc --noEmit` 验证。
- 修复后更新 `CHANGELOG.md` 与 `CLAUDE.md` (如有必要)。

## 2. 全项目 reviewer 状态

| 维度 | 状态 | 来源 |
|------|------|------|
| Backend / API | ✅ | `buuro-backend` agent |
| Frontend / UI | ✅ | `buuro-frontend` agent |
| Security / OWASP | ✅ | `security-reviewer` agent |
| Infra / release | ✅ 自检 | 429 失败, 已用自检覆盖 |
| Performance | ⏳ 跑中 | `performance-reviewer` |
| Redundancy | ⏳ 跑中 | `refactor-cleaner` (原本的 `ponytail:ponytail-audit` 改用此 agent) |

**Ponytail 状态栏** 还没配 `~/.claude/settings.json`, 我之后会一次性给你贴一段。

## 3. 修复分组 (按 reviewer 与 Ponytail 强度排序)

### P0 — 必修 (正确性 / 安全)

| # | 来源 | 文件 | 改动 | 行数 |
|---|------|------|------|------|
| 0a | S6 | `server/utils/auth.ts:8` | 生产 `SESSION_SECRET` 缺失时启动抛错 | ~5 |
| 0b | S2 | `server/plugins/02-bot-setup.ts:10-12` | 生产 `BOT_WEBHOOK_SECRET` 缺失则 `setWebhook` 不注册 | ~5 |
| 0c | B1 | `server/routes/api/posts/[id].patch.ts:21-26` | 删重复 `db.update`, 保留 `returning`; 加 API key 旁路注释 | 2 |
| 0d | B2 | `server/routes/api/posts/[id]/tags.put.ts:25-39` | 改用 `.returning()`, 仅在真插入时 `+1` | ~6 |
| 0e | B3 | `server/routes/logout.post.ts:7-13` | 删 `$fetch` 自调用, 直接 `deleteCookie` | ~5 |
| 0f | B4 | `server/utils/auth.ts` + 三处调用 | 抽 `setSessionCookie(event,token)` / `clearSessionCookie(event)`, 统一 `secure/sameSite/path` | +12 / -3 |
| 0g | B6 / B9 | `server/routes/api/tasks/index.post.ts`, `server/routes/api/tasks/web-import.post.ts` | 接入 `isPrivateHost` (已存在) | ~3 / 文件 |
| 0h | B7 | `server/routes/api/tags/index.get.ts:10-14` | 显式 `if (path === 'autocomplete') return` 让位 | 2 |
| 0i | F1 | `app/layouts/default.vue:156` + `AccentPicker.vue:45` + `ThemeToggle.vue:50` | `active:scale-85` → `active:scale-90` | 3 |

### P1 — 推荐 (可读性 / 一致性 / 死代码)

| # | 来源 | 文件 | 改动 | 行数 |
|---|------|------|------|------|
| 1a | B11 | `server/routes/api/admin/tags/aliases.post.ts` | 加 alias_name 唯一性预检 → 409 | ~6 |
| 1b | B dead | `server/routes/api/tasks/[id].get.ts` | 删 `safeResult.metadata.phash` 死分支 | 2 |
| 1c | F dead | `app/composables/ai.ts` | 整个文件 0 引用, 删 | ~50 |
| 1d | F dead | `app/components/FilterPills.vue` | 0 引用, 删 | ~20 |
| 1e | F2 | `error.vue` | 移到 `app/error.vue` (走 Nuxt 主题/布局) | mv |
| 1f | F3 | `app/pages/posts/[id].vue:157` | `@click.native.prevent` → `@click.prevent` | 1 |
| 1g | F2 hardcode | `app/layouts/default.vue:60-61` 与 `AccentPicker.vue:11` | `accentHue + 25` 抽常量 `ACCENT_HUE_SPAN` | ~3 |
| 1h | F9 | `app/components/admin/TagsPanel.vue:26-32` | `ai_status` 改成 URL query, 让后端分页参与 | ~6 |
| 1i | F4 | `app/pages/search.vue:19-30` | `pushRecent` 改 `watchDebounced` + dedupe, `useAsyncData` 去重 | ~6 |

### P2 — known-issues (不修, 留 PR/issue)

- F5 `PhotoCard` LQIP `onMounted` 闪烁 → 直接 `v-if="lqip"`。
- F6 `posts/[id].vue` 右 aside + 移动 info block 抽 `<PostInfoPanel>` (改动大, 留 issue)。
- F7 layout 全局与 detail 页 J/K 双绑 — 保留页面级, layout 默认 no-op 即可。
- F8 `random.vue` Space 键 (边缘) — 不动。
- F10 `kura-per-page` cookie 缺 `SameSite` — 一起合并到 P0f 抽 helper。
- S1 (HIGH) API key = admin: 改 scope (窄端点 + 签名 job id) — **架构改动**, 留 issue, P0c 仅加注释。
- S3 (MED) sidecar SSRF redirect 收紧 (max-redirects=1, IP pin) — 留 issue。
- S4 (MED) `isPrivateIPv4` 补网络段 — 留 issue。
- S5 (MED) 登录限流 — 留 issue。
- S7 (LOW) `head_inject` 仅 admin viewer — 留 issue。
- S8 (LOW) `sanitizeDescriptionHtml` → DOMPurify — 留 issue。
- S9 (LOW) 安全响应头中间件 — 留 issue。
- S10 (LOW) CORS extension origin 白名单 — 留 issue。
- 自检 P2: `getRandomPost` 5min count 缓存 / `i/[...].ts` 缺 `Range` / `/api/posts?q=` 与 `/api/search` 重复 / `bot-rating.ts` L 字面量与 i18nLabels 重复 — 全留 known-issues。

## 4. 验证

- **类型检查**: `npx vue-tsc --noEmit` 必过。
- **构建**: `npx nuxt build` 必过 (无新 .vue 错误)。
- **无需测试框架**: 仓库未配 vitest, 跳过。

## 5. 不修的范围

- v0.7.6 已经发布了; 任何破坏性改动都不做。
- `nuxt.config.ts`、`Dockerfile`、`docker-compose.yml`、`sidecar/sidecar.py` 不动 (无 P0/P1)。
- 所有文档 (除 `CHANGELOG.md` 与 `CLAUDE.md` 必要的同步) 不动。

## 6. 修复执行顺序 (由低风险到高)

1. P1 死代码/重命名/小改 (1b-1g) — 不影响行为。
2. P1 TagsPanel + search debounce (1h, 1i) — UX。
3. P0 修本地 bug (0c, 0d, 0e, 0h, 0i)。
4. P0 安全守卫 (0a, 0b, 0f) — 启动行为, 必先与用户确认。
5. P0 SSRF (0g)。
6. 跑 `npx vue-tsc --noEmit` + `npx nuxt build` 验证。
7. 写 `CHANGELOG.md` v0.7.7 entry, 提交一个 PR。

## 7. 我需要你确认的两点

- S1 (HIGH) 改不改: API key = admin 这条要动吗? 急推版建议仅在 `posts/[id].patch.ts` 文件头加 P1 注释 + 留 issue, 后续单独 PR 拆端点; 你定。
- P0f cookie helper 要不要做: 因为改 3 个文件 + 1 个 helper, 改动面中等; 但与 F10 缺 `SameSite` 一起改最经济, 我倾向"做"。

不动代码, 等你确认 (或提修改) 后我按 §6 顺序执行, 全程 `npx vue-tsc --noEmit` 必过。
